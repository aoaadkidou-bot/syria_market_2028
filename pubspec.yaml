name: syria_market_2028
description: "تطبيق سوق سوريا الشامل 2028"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # الأيقونات
  cupertino_icons: ^1.0.6

  # إدارة الحالة والخدمات السحابية
  supabase_flutter: ^2.5.0
  shared_preferences: ^2.2.3

  # الصور والمشاركة والروابط
  image_picker: ^1.1.1
  share_plus: ^9.0.0
  url_launcher: ^6.2.6

  # 🎙️ التعرف الصوتي والأذونات
  speech_to_text: ^6.6.0
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true